{
  "name": "solidity-gas-lens",
  "displayName": "Solidity Gas Lens",
  "description": "Live gas-cost inlay hints for Solidity functions, measured against a local Anvil node.",
  "publisher": "local-dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/extension.js",
  "engines": {
    "vscode": "^1.85.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Solidity Gas Lens",
      "properties": {
        "gasLens.enabled": {
          "type": "boolean",
          "default": true
        },
        "gasLens.anvilPort": {
          "type": "number",
          "default": 8545
        },
        "gasLens.foundryMode": {
          "type": "boolean",
          "default": false,
          "description": "Not yet implemented."
        },
        "gasLens.gasIncreaseThreshold": {
          "type": "number",
          "default": 5,
          "description": "Not yet wired up; the 5% threshold is currently hardcoded in src/cache.ts."
        },
        "gasLens.gasDecreaseThreshold": {
          "type": "number",
          "default": 5,
          "description": "Not yet wired up; the 5% threshold is currently hardcoded in src/cache.ts."
        },
        "gasLens.showOnlyPublic": {
          "type": "boolean",
          "default": false
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && echo '{\"type\":\"commonjs\"}' > dist/package.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@solidity-parser/parser": "^0.19.0",
    "ethers": "^6.13.4",
    "solc": "^0.8.28"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
